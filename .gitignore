# macOS
.DS_Store
.AppleDouble
.LSOverride

# Archives that shouldn't ship
*.zip
*.tar
*.tar.gz

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local install cache
.cache/

# Local dev artifacts
*.local.md
*.local.json
# Maintainer-only tooling (lives at repo root, not shipped publicly)
/scripts/

# maintainer/agent working notes — local only, never published
/AGENTS.md
/CLAUDE.md

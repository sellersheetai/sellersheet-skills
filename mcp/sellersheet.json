{
  "_comment_": "SellerSheet MCP is a HOSTED remote server (streamable HTTP) — there is nothing to install locally. Paste this into your agent's MCP config file, or use your agent's one-command add (see _per_agent_ below). Get an API key at https://sellersheetai.com/dashboard → MCP & API keys → Create Key.",
  "mcpServers": {
    "sellersheet": {
      "type": "http",
      "url": "https://sellersheetai.com/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY"
      }
    }
  },
  "_per_agent_": {
    "claude-desktop": "Settings → Connectors → Add custom connector → URL https://sellersheetai.com/mcp (OAuth, no key needed)",
    "claude-code": "claude mcp add-json sellersheet '{\"type\":\"http\",\"url\":\"https://sellersheetai.com/mcp\",\"headers\":{\"Authorization\":\"Bearer YOUR_API_KEY\"}}'",
    "codex": "codex mcp add sellersheet --url https://sellersheetai.com/mcp   (browser OAuth on first use — no key needed; config.toml uses http_headers, NOT headers, if you set a Bearer manually)",
    "codebuddy-code": "Plugin route (recommended, keyless): /plugin marketplace add sellersheetai/sellersheet-skills then /plugin install sellersheet-skills@sellersheet-marketplace — registers the server, sign in via /mcp. Manual route: codebuddy mcp add-json --scope user sellersheet '{\"type\":\"http\",\"url\":\"https://sellersheetai.com/mcp\",\"headers\":{\"Authorization\":\"Bearer YOUR_API_KEY\"}}'  (腾讯云 CodeBuddy Code：推荐用插件方式安装，无需 API key；手动方式见左侧命令)",
    "cursor": "~/.cursor/mcp.json — mcpServers block above without the \"type\" field",
    "windsurf-antigravity": "use \"serverUrl\" instead of \"url\" in the block above",
    "gemini-cli": "~/.gemini/settings.json — httpUrl + headers",
    "generic": "any MCP client that supports streamable HTTP: url + Authorization Bearer header"
  },
  "_setup_steps_": [
    "1. Sign in at https://sellersheetai.com",
    "2. Dashboard → MCP & API keys → Create Key (skip for OAuth clients like Claude Desktop / codex mcp add)",
    "3. Register the server per _per_agent_ above",
    "4. Restart your agent",
    "5. Ask the agent to run get_user_context to confirm the connection"
  ]
}
